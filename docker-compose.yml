services:
  express:
    build:
      context: .
      dockerfile: Dockerfile
    image: express
    container_name: express-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    command: npm run start
    env_file:
      - ./.env.production
    network_mode: host